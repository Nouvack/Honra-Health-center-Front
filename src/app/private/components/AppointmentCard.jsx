"use client"

export default function AppointmentCard({ appointment }) {
    return (
        <div className="w-100 h-100 border-1 border-[var(--turquoise)] rounded-3xl">
            
        </div>
    )
}